# 필수사항 + 미충족 상세 정리

현재 코드 기준으로, 과제 원문상 사실상 꼭 필요한데 아직 완전히 만족하지 못하는 항목만 정리했습니다.

## 1. 레코드 추가 시 ID가 자동 부여된다

- 판정: 미충족
- 필수인 이유:
  - 과제 본문에 "테이블에 레코드가 추가되면 ID가 자동으로 부여"된다고 직접 명시되어 있습니다.
- 현재 미충족인 이유:
  - `id`를 생략한 컬럼 지정 INSERT는 자동 ID가 동작합니다.
  - 하지만 모든 INSERT 형태에서 자동 ID를 보장하지는 않습니다.

## 2. 자동 부여 ID가 모든 INSERT 형태에서 일관되게 동작한다

- 판정: 미충족
- 필수인 이유:
  - 자동 ID 기능은 특정 SQL 형태에서만 부분적으로 동작하는 것이 아니라, 과제에서 요구하는 레코드 추가 전반에 대해 일관되게 제공되어야 합니다.
- 현재 미충족인 이유:
  - `columns == NULL`인 일반 VALUES 형태 전체를 자동 ID 대상으로 처리하지 않습니다.
  - 즉, 일부 INSERT 형태에서는 자동 ID가 되지 않습니다.

## 3. 자동 ID가 연속 증가 규칙을 따른다

- 판정: 미충족
- 필수인 이유:
  - 과제에서 ID 자동 부여를 요구하는 이상, 자동 생성되는 ID는 중복 없이 예측 가능한 증가 규칙을 만족해야 합니다.
- 현재 미충족인 이유:
  - 지원되는 경로에서는 `next_id` 기반으로 증가합니다.
  - 하지만 모든 삽입 경로에 대해 같은 규칙이 일관되게 적용된다고 보기 어렵습니다.
  - 따라서 전면 충족으로 보기에는 부족합니다.

## 4. 최소 1,000,000개 이상의 레코드를 Insert한다

- 판정: 미충족
- 필수인 이유:
  - 과제 본문에 "최소 1,000,000개 이상의 레코드를 Insert"하라고 직접 명시되어 있습니다.
- 현재 미충족인 이유:
  - 현재 SQL 인덱스 벤치의 기본 row 수는 `100`, `1000`, `5000`입니다.
  - 실제 INSERT 경로(`storage_insert`)를 사용하는 점은 긍정적이지만, 100만 건 기준은 충족하지 못합니다.

## 5. 대규모 데이터 삽입 후 성능 테스트를 수행한다

- 판정: 미충족
- 필수인 이유:
  - 과제 본문에 "대규모 데이터 삽입 후 성능 테스트"를 수행하라고 명시되어 있습니다.
- 현재 미충족인 이유:
  - ID 검색과 비-ID 검색의 성능 비교 코드는 존재합니다.
  - 하지만 현재 데이터 규모는 과제 요구 수준인 대규모 삽입 테스트라고 보기 어렵습니다.

## 6. 데이터 규모 증가에 따른 성능 차이를 충분히 보여준다

- 판정: 미충족
- 필수인 이유:
  - 과제는 단순 기능 확인이 아니라, 대규모 데이터에서 인덱스와 선형 탐색의 차이를 비교해보라고 요구합니다.
  - 따라서 충분히 큰 데이터 구간에서 차이를 보여주는 것이 사실상 필요합니다.
- 현재 미충족인 이유:
  - 현재 비교 구간은 `100`, `1000`, `5000`으로 작습니다.
  - 과제의 "대용량 성능 비교"를 설득력 있게 보여주기에는 부족합니다.

## 요약

- 미충족 1: 레코드 추가 시 ID 자동 부여 전면 지원
- 미충족 2: 자동 부여 ID의 모든 INSERT 형태에 대한 일관성
- 미충족 3: 자동 ID 증가 규칙의 전면 보장
- 미충족 4: 최소 1,000,000개 이상의 실제 Insert 수행
- 미충족 5: 대규모 데이터 삽입 후 성능 테스트
- 미충족 6: 데이터 규모 증가에 따른 성능 차이의 충분한 입증
