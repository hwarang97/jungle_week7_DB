# B+ Tree 정렬 Phase 검증 퀴즈

정렬 Phase(11:00~12:00) 끝나고 전원이 풀어보세요.
종이에 직접 그리거나 말로 설명하는 방식으로 진행합니다.
**제한시간: 15분**

---

## Part 1. 개념 확인 (말로 답하기)

### Q1.
B+ Tree에서 실제 데이터(레코드 포인터)는 어디에 저장되는가?
내부 노드에는 뭐가 저장되는가?

### Q2.
B+ Tree의 리프 노드끼리 연결되어 있는 이유는 뭔가?
이게 없으면 어떤 작업이 어려워지는가?

### Q3.
B+ Tree에서 키를 검색할 때 시간복잡도는?
100만 개 데이터가 있고 order가 100이면 최대 몇 번 노드를 방문하는가?

### Q4.
B Tree와 B+ Tree의 차이를 한 문장으로 설명하라.

---

## Part 2. 손으로 그리기 (종이에 직접)

### Q5.
order = 3인 B+ Tree에 아래 키를 순서대로 삽입하라.
매 분할이 일어날 때마다 트리 전체 모양을 그려라.

삽입 순서: **1, 2, 3, 4, 5**

(1) 1 삽입 후 트리 모양
(2) 2 삽입 후 트리 모양
(3) 3 삽입 후 트리 모양 ← 여기서 분할 발생
(4) 4 삽입 후 트리 모양
(5) 5 삽입 후 트리 모양 ← 여기서 또 분할 발생

### Q6.
Q5에서 완성된 트리에서 **키 3**을 검색하는 경로를 화살표로 표시하라.
루트 노드에서 시작해서 어떤 비교를 거쳐 어떤 자식으로 내려가는지 단계별로 적어라.

### Q7.
Q5에서 완성된 트리에 **키 6**을 삽입하면 어떻게 되는가?
분할이 발생하는가? 발생한다면 트리 모양을 그려라.

---

## Part 3. SQL 연동 사고 (말로 답하기)

### Q8.
아래 SQL이 실행될 때, B+ Tree가 관여하는 시점은 각각 언제인가?

```sql
INSERT INTO users VALUES (1, 'jiun', 25);
SELECT * FROM users WHERE id = 3;
SELECT * FROM users WHERE name = 'sein';
```

세 쿼리 중 B+ Tree를 사용하는 것과 사용하지 않는 것을 구분하고 이유를 말하라.

### Q9.
기존 SQL 처리기 코드에서 B+ Tree를 연동하려면 최소한 어떤 위치에 코드를 추가해야 하는가?
"INSERT 처리 함수의 _____ 시점"
"SELECT 처리 함수의 _____ 시점"
빈칸을 채워라.

---

## Part 4. 의사코드 읽기

### Q10.
아래 insert 의사코드에서 (A), (B), (C)에 들어갈 내용을 말하라.

```
function insert(tree, key, value):
    leaf = find_leaf(tree, key)
    
    if leaf의 키 개수 < (A):
        leaf에 (key, value) 삽입
    else:
        (B)
        new_key = new_leaf의 첫 번째 키
        (C)
```

### Q11.
아래 search 의사코드에서 (D), (E)에 들어갈 내용을 말하라.

```
function search(tree, key):
    node = tree.root
    
    while node가 (D):
        node의 키들을 순회하며
        key < node.keys[i] 인 첫 번째 i를 찾음
        node = (E)
    
    node(리프)에서 key와 일치하는 값을 반환
```

---

## 채점 기준

- Part 1 (Q1~Q4): 4문제, 개념을 자기 말로 설명할 수 있으면 OK
- Part 2 (Q5~Q7): 3문제, 트리 모양이 정확하면 OK
- Part 3 (Q8~Q9): 2문제, 연동 지점을 특정할 수 있으면 OK
- Part 4 (Q10~Q11): 2문제, 의사코드 흐름을 이해하면 OK

**8/11 이상 맞으면**: 사이클 진입 준비 완료
**5~7개**: 틀린 부분만 빠르게 보충하고 진입
**4개 이하**: 정렬 Phase를 30분 연장하고 개념 재정리
